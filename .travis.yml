language: python
python:
  - "2.7"
  #- "3.4"
  #- "3.5"
  #- "3.6"
  # PyPy versions, TODO: enable later
  #- "pypy"
  #- "pypy3"
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
#command to run tests
script: make test